<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_review_list" model="ir.ui.view">
        <field name="name">odex.road.mechanic.review.list</field>
        <field name="model">odex.road.mechanic.review</field>
        <field name="arch" type="xml">
            <list string="Reviews" decoration-warning="state == 'pending'" decoration-muted="state == 'rejected'">
                <field name="create_date" string="Submitted"/>
                <field name="workshop_id"/>
                <field name="customer_name"/>
                <field name="rating"/>
                <field name="stars" string="Stars"/>
                <field name="title" optional="show"/>
                <field name="state" widget="badge"
                       decoration-success="state == 'approved'"
                       decoration-warning="state == 'pending'"
                       decoration-danger="state == 'rejected'"/>
                <button name="action_approve" type="object" string="Approve" icon="fa-check"
                        invisible="state == 'approved'"/>
                <button name="action_reject" type="object" string="Reject" icon="fa-times"
                        invisible="state == 'rejected'"/>
            </list>
        </field>
    </record>

    <record id="view_review_form" model="ir.ui.view">
        <field name="name">odex.road.mechanic.review.form</field>
        <field name="model">odex.road.mechanic.review</field>
        <field name="arch" type="xml">
            <form string="Review">
                <header>
                    <button name="action_approve" type="object" string="Approve" class="btn-primary"
                            invisible="state == 'approved'"/>
                    <button name="action_reject" type="object" string="Reject"
                            invisible="state == 'rejected'"/>
                    <button name="action_reset_pending" type="object" string="Reset to Pending"
                            invisible="state == 'pending'"/>
                    <field name="state" widget="statusbar" statusbar_visible="pending,approved"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1><field name="stars" readonly="1"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="workshop_id"/>
                            <field name="customer_name"/>
                            <field name="customer_email" widget="email"/>
                            <field name="partner_id"/>
                        </group>
                        <group>
                            <field name="rating"/>
                            <field name="approved" readonly="1"/>
                            <field name="create_date" readonly="1"/>
                            <field name="active"/>
                        </group>
                    </group>
                    <group>
                        <field name="title"/>
                        <field name="comment"/>
                        <field name="moderation_notes"/>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_review_search" model="ir.ui.view">
        <field name="name">odex.road.mechanic.review.search</field>
        <field name="model">odex.road.mechanic.review</field>
        <field name="arch" type="xml">
            <search string="Reviews">
                <field name="workshop_id"/>
                <field name="customer_name"/>
                <filter name="filter_pending" string="Pending" domain="[('state', '=', 'pending')]"/>
                <filter name="filter_approved" string="Approved" domain="[('state', '=', 'approved')]"/>
                <filter name="filter_rejected" string="Rejected" domain="[('state', '=', 'rejected')]"/>
                <group expand="0" string="Group By">
                    <filter name="group_workshop" string="Workshop" context="{'group_by': 'workshop_id'}"/>
                    <filter name="group_state" string="Status" context="{'group_by': 'state'}"/>
                    <filter name="group_rating" string="Rating" context="{'group_by': 'rating'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_review" model="ir.actions.act_window">
        <field name="name">All Reviews</field>
        <field name="res_model">odex.road.mechanic.review</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_review_search"/>
    </record>

    <record id="action_review_pending" model="ir.actions.act_window">
        <field name="name">Pending Reviews</field>
        <field name="res_model">odex.road.mechanic.review</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('state', '=', 'pending')]</field>
        <field name="search_view_id" ref="view_review_search"/>
    </record>

    <record id="action_review_approved" model="ir.actions.act_window">
        <field name="name">Approved Reviews</field>
        <field name="res_model">odex.road.mechanic.review</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('state', '=', 'approved')]</field>
        <field name="search_view_id" ref="view_review_search"/>
    </record>

    <record id="action_review_rejected" model="ir.actions.act_window">
        <field name="name">Rejected Reviews</field>
        <field name="res_model">odex.road.mechanic.review</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('state', '=', 'rejected')]</field>
        <field name="search_view_id" ref="view_review_search"/>
    </record>

</odoo>
